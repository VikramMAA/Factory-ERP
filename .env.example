# Client-side. These ARE bundled into the JS and are public by design.
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGci...
VITE_PHOTO_MAX_EDGE_PX=800
VITE_PHOTO_JPEG_QUALITY=0.6

# Never-bundled secrets (SUPABASE_DB_URL, SUPABASE_SERVICE_ROLE_KEY,
# SUPABASE_PROJECT_REF) live only as GitHub repository secrets / Supabase
# function secrets. Never add them here with a VITE_ prefix — see
# vite.config.ts for the build-time guard that enforces this.
